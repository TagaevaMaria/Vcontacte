import 'package:flutter/material.dart';







/// тут тренируюсь и учусь
class ScreenClips extends StatelessWidget {
  const ScreenClips({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {

    return  Scaffold(body:
      Center(child: TextButton(onPressed: (){}, child: Text('Нажми', style: TextStyle(fontSize: 30),))),

    );
  }
}


